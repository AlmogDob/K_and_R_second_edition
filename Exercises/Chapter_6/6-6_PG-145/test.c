#include <stdio.h>

enum types
{
    DEFETION,
    INSTALLATION,
};

int main(int argc, char const *argv[])
{
    printf("%d", INSTALLATION);
    return 0;
}

outside of else if        
*w = g
1
*w =
word = almog
